Star word — a collected word pinned to the Language Wall like a postcard. Lowercase italic, pigment pin, slight hang.

```jsx
<StarWord word="wobbly" pin="river" rotate={-2} />
<StarWord word="enormous" pin="marigold" rotate={1.5} />
```
