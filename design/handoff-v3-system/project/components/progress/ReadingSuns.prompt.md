Reading-day suns on the sill — drawn suns that only accumulate. No empty slots, no broken streaks; the newest one glows.

```jsx
<ReadingSuns count={4} />
<ReadingSuns count={7} size={36} sill={false} />  // wall-calendar style
```
