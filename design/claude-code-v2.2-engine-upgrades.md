# Claude Code prompt — v2.2 engine upgrades (Azi-Verse architecture adoption)

Run after the v2.1 fix round. Spec: `docs/aziverse-adoption.md` (binding) with source docs in `docs/reference/azi-verse/` (`azi-verse-app-architecture.md` has the exact data models — use its JSON shapes). This round is engine + parent-side; kid-facing UI changes are limited to items 6–7.

Implement adoption items **1–7** in order:

1. **Two-stage QA** in `/api/story`: deterministic pre-checks (band word counts, heritage-word density, profile `excludeTerms`) → hard-gate LLM judge (character consistency vs. bible, cultural sensitivity, age match, cultural accuracy; any fail → regenerate with violation notes, max 2) → soft scoring with the doc's `scoringWeights`, <90 → one revision loop. Persist `qaRecord` per the doc's Story model; show gates/score/revisions in Parent Corner story detail.
2. **Embodiment prompt assembly**: refactor the system prompt into the 7-step order (narrator identity block from universe config → child profile context → universe payload scoped to selected characters/settings → cultural config → band spec → skill target → optional seed). Add `narratorIdentity`, `hardRules`, `scoringWeights` to `azad-verse.ts` universe config, seeded from `universe-guide.md` tone/rituals.
3. **Character schema upgrade** in the universe config and `content/art/characters.json`: `traits`, `speechPatterns`, `relationships`, `canonRules`, `visualAnchors`, `roleByBand` — populate for Azi + all plush companions from the universe guide (the architecture doc's Jujy example is the template). Generation payload includes canonRules + speechPatterns for characters in the story; the QA character-consistency gate validates against them.
4. **Child profile** (`lib/read/profile.ts` + Parent Corner "Profile" section): the doc's ChildProfile shape — band, languages (home/heritage/exposureGoals), interests, currentChallenges, comfortObjects, contentPreferences (excludeTerms, toneCalibration, framingDevices). Feeds prompt step 2 and deterministic QA. Migrate the existing universe-editor interests into it; keep no-last-name/no-photo/local-only rules as code comments and behavior.
5. **Skill tagging**: SS-taxonomy ids (from `docs/reference/azi-verse/future-ready-skills.md`) tagged on every generated book (model returns them; validated against the taxonomy) and backfilled onto pack-000 via a one-time script pass mapping existing `teachingGoals`.
6. **Mystery Word + Language Wall** (kid-facing): each book designates one heritage star word as its Mystery Word; tapping it in the reader triggers a small found-it celebration and adds it to the **Language Wall** — a new section at the top of My Words showing family words collected, grouped by language. Backfill mystery words for pack-000 (bhen, agua-class words where present).
7. **Comfort-ritual closing beat** (kid-facing): before the book-complete celebration, one quiet interstitial in the story's closing register (moon/snack/song motif per universe rituals; skip for stories that already end on their own ritual page). ≤4s, auto-advances, reduced-motion safe.

Constraints: no regressions to the v2.1 checklist; all new API behavior degrades gracefully offline (QA only runs at generation time anyway); Parent Corner remains plain/quiet; nothing here blocks reading.

Verify: generate a story via StoryMaker → observe qaRecord with gates + breakdown in Parent Corner; add "agua" to excludeTerms → regenerate → gate rejects/strips it; Jujy story attempt where he acts cruel → hard gate fails with violation note; profile edit changes next generation's tone; Miko's mystery word found → Language Wall shows it; a finished book shows the closing beat then celebration; pack-000 books show skill tags in Parent Corner.
